<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate TargetType="Control" x:Key="BaseTemplate">
        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
            <ScrollViewer x:Name="PART_ContentHost"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="Yellow"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="Yellow"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="Control" x:Key="BaseStyle">
        <Setter Property="FontFamily" Value="{StaticResource Font.Workbench.Slight}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Black"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template" Value="{StaticResource BaseTemplate}"/>
    </Style>

    <Style TargetType="Control" x:Key="SpecialStyle" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="Yellow"/>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SelectionBrush" Value="DarkGray"/>
    </Style>

    <Style TargetType="TextBox" x:Key="TextBox.Special" BasedOn="{StaticResource SpecialStyle}">
        <Setter Property="CaretBrush" Value="Yellow"/>
        <Setter Property="SelectionBrush" Value="DarkGray"/>
    </Style>

    <Style TargetType="PasswordBox" BasedOn="{StaticResource SpecialStyle}">
        <Setter Property="PasswordChar" Value="*"/>
        <Setter Property="CaretBrush" Value="Yellow"/>
        <Setter Property="SelectionBrush" Value="DarkGray"/>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}" x:Key="TextBox.Medium">
        <Setter Property="FontFamily" Value="{StaticResource Font.Workbench.Evenly}"/>
        <Setter Property="FontSize" Value="18"/>
    </Style>
</ResourceDictionary>