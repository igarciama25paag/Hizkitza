<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="BaseInputTemplate" TargetType="Control">
        <Border x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}">
            <ScrollViewer x:Name="PART_ContentHost"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="Yellow"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="Yellow"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="BaseInputStyle" TargetType="Control">
        <Setter Property="FontFamily" Value="{StaticResource Font.Workbench.Slight}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="Yellow"/>
        <Setter Property="Background" Value="Black"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="Template" Value="{StaticResource BaseInputTemplate}"/>
    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource BaseInputStyle}">
        <Setter Property="CaretBrush" Value="Yellow"/>
    </Style>
    <Style TargetType="PasswordBox" BasedOn="{StaticResource BaseInputStyle}">
        <Setter Property="PasswordChar" Value="*"/>
        <Setter Property="CaretBrush" Value="Yellow"/>
    </Style>
</ResourceDictionary>